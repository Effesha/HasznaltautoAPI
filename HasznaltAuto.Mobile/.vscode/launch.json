{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "edge",
      "request": "launch",
      "name": "localhost (Edge)",
      "url": "http://localhost:56863",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
